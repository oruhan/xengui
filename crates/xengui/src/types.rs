/// Convenient alias for `Rect`.
pub type Rect = (f32, f32, f32, f32);
/// Convenient alias for `Point`.
pub type Point = (f32, f32);
/// Convenient alias for `Triangle`.
pub type Triangle = (Point, Point, Point);
